import ToolPanel from "../components/ToolPanel";

function PagesRoute() {
  return (
    <ToolPanel
      title="Edit Pages"
      description="Remove or rotate pages. The original file is never changed."
    >
      <p className="text-xs text-neutral-400">Not built yet — next phase.</p>
    </ToolPanel>
  );
}

export default PagesRoute;
