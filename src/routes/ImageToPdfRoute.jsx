import ToolPanel from "../components/ToolPanel";

function ImageToPdfRoute() {
  return (
    <ToolPanel
      title="Image → PDF"
      description="Turn PNG or JPEG images into an A4 PDF, then send it straight to the printer."
    >
      <p className="text-xs text-neutral-400">Not built yet — next phase.</p>
    </ToolPanel>
  );
}

export default ImageToPdfRoute;
